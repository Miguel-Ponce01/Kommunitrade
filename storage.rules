rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read for item images
      // Allow write only for authenticated users (sellers)
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
